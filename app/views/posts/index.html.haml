%h2 All posts
%ul
  %table
    %tbody
      - @posts.each do |post|
        %tr
          %td= post.created_at.strftime("%Y-%m-%d")
          %td
            %span{style: "font-weight: 600;"}= link_to post.title, post_path(post)
